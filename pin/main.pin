// import the module containing the interpreter
let lang = import("src/mod.pin");


let main =
  if len(args) <= 0 then
    "Expected a file path to interpret"
  else
    let file_path = args[0];
    lang.interpret(file_path);


main
