let lexer  = import("lexer.pin");
let parser = import("parser.pin");


let interpret(path) =
  let source = read_file(path);
  let tokens = lexer.lex(source);
  if not tokens.ok then
    tokens.errors

  else
    let tree = parser.parse(tokens.tokens);
    tree;


{ interpret = interpret
}